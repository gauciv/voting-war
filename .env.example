# Frontend environment variables
# These are baked into the build at `npm run build` time.

# Point to your EC2 backend (replace with your EC2 public IP or domain)
VITE_API_URL=http://<EC2_PUBLIC_IP>:3000/api
VITE_WS_URL=ws://<EC2_PUBLIC_IP>:3000/api/ws
