# ===== Deployment — only set what you need =====
# Comma-separated frontend origins (default: localhost dev servers)
# CORS_ORIGINS=https://your-frontend.com

# AWS credentials (leave blank to use IAM role on EC2)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# DynamoDB table name (default: voting-war-scores)
# DYNAMODB_TABLE_NAME=voting-war-scores
